{
  "name": "pula-loans",
  "private": true,
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "start": "cd backend && node src/server.js",          // ← ADD THIS LINE
    "build": "cd frontend && npm run build"             // ← AND THIS ONE
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}